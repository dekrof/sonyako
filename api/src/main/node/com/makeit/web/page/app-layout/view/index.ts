import { LocaleProvider, LocaleSwitcher } from "@page/app-layout/lang";
import { Title, TitleTarget } from "@page/app-layout/view/title";
import Footer, {FooterTarget} from "@page/app-layout/view/footer";

export { LocaleSwitcher, LocaleProvider, TitleTarget, Title, Footer, FooterTarget }
