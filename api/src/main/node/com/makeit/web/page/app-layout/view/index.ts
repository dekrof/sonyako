import { LocaleProvider, LocaleSwitcher } from "@page/app-layout/lang";
import { Title, TitleTarget } from "@page/app-layout/view/title";

export { LocaleSwitcher, LocaleProvider, TitleTarget, Title }
